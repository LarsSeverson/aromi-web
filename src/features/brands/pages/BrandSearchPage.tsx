import React from 'react'

export interface BrandSearchPageProps {
  term: string
}

const BrandSearchPage = (props: BrandSearchPageProps) => {
  return (
    <div>

    </div>
  )
}

export default BrandSearchPage
